# Target interpreter: Python 3.10.x  (3.10.11 verified)
#
# Two pins from the original file cannot be installed on Python 3.10 and were
# bumped to the nearest release that publishes a cp310 wheel:
#   pandas 1.3.0 -> 1.3.5   (1.3.0/1.3.1/1.3.2/1.3.3 are cp39-max)
#   shap   0.40.0 -> 0.41.0 (0.40.0 is cp39-max)
category_encoders==2.5.0
# Config files may be YAML or JSON; ml_pipeline/config.py documents both, so
# this is a runtime dependency rather than an optional extra.
pyyaml==6.0.2
hyperopt==0.2.7
lightgbm==3.3.0
matplotlib==3.5.1
numpy==1.22.4
pandas==1.3.5
scikit_learn==1.1.2
seaborn==0.12.0
shap==0.41.0
